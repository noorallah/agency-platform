# Defect register

One list of every known defect: what is open, and what was fixed, when and
where. Started on 2026-09-18, because defects were being recorded in three
places -- `docs/BACKLOG.md` §31, the "Known defects" notes at the end of each
section of `docs/INDEPENDENT_TEST_CASES.md`, and PR descriptions -- and nobody
could answer "is there anything open?" without reading all three.

**This file is the index; the detail stays where it was found.** Each row
links to the full write-up. Status here was checked against the code on
2026-09-18 unless the row says otherwise.

## How to use it

- **Found a defect:** add a row to **Open** with the next id for its area
  (`D-BUY-9`, `D-SELL-1`, ...), the date, a one-line summary, the evidence, and
  a link to where it is written up. Keep an existing id when it already has one
  (`D-27-4`, `BL-31.13`).
- **Fixed it:** move the row to **Fixed**, add the date, the PR, and the test
  that fails when the fix is reverted. A fix without such a test is not done.
- **Decided it is not a defect:** move it to **Not a defect** with the reason.
- **Severity.** *High* -- wrong money, stock or tax, or a rule that can be
  bypassed. *Medium* -- a wrong record or a broken screen with a workaround.
  *Low* -- wording, a missing label, a convenience.
- **Evidence.** *Live* -- seen in real rows or on screen. *Code* -- read off the
  code and not yet driven. Drive a *Code* row before fixing it.

---

## Open

### Buying -- found writing the Buying data trail, 2026-09-18

Full write-ups: PR #435 and `docs/DATA_TRAIL_BY_OPERATION.md` §9.

| Id | Severity | Summary | Evidence |
| --- | --- | --- | --- |
| D-BUY-8 | Low | Three small ones: a supplier advance can never be allocated after it is recorded; receiving that moves an order's status writes an audit row but no history row; an edit's two history rows share one timestamp. | Code |

### Stock -- found writing the Stock data trail, 2026-09-18

Full write-ups: the PR that added `docs/DATA_TRAIL_BY_OPERATION.md` §10, and
the section itself. BL-31.13 (the Stock Ledger's type filter) already covers
the filter and is not repeated here.

| Id | Severity | Summary | Evidence |
| --- | --- | --- | --- |
| D-STK-5 | Low | **A count with nothing counted can be posted**, reading POSTED with `adjusted_lines` 0 (`PhysicalCountService.post` has no "at least one counted line" check). | Live: WHOLE01 `PC-2026-2027-000005` |
| D-STK-6 | Low | **Two writes with no named audit row:** a quarantine hold or release leaves only `inventory.transaction.created` (`quarantine_stock` calls no `record_audit`), and Save progress on a count (`PhysicalCountService.update`) writes none. | Live (the hold's request holds one audit row); Code |
| D-STK-7 | Low | **A cancelled order's release is dated today (UTC) while its reservation is dated the order date** (`sales_order_service.py`, `_release_inventory` vs `_reserve_inventory`), so the ledger shows the release before the hold. | Live: WHOLE01 SO-2026-2027-000015 to 000018, reserved 2026-09-13, released 2026-09-12 |
| D-STK-8 | Low | **Expiry Monitor's "Expired Today" and "Total Expired" are the same count** (`expiry_dashboard` evaluates `expired_condition(today)` for both), and every card counts `batches` rows whether or not any stock is left. | Code |
| D-STK-9 | Low | **A reversing movement is dated the original's `transaction_date`** (`reverse_transaction`), so the stock ledger by date shows the goods leaving the day they arrived and `inventories.last_transaction_at` goes backwards -- the stock-side twin of D-BUY-4. | Live: WHOLE01 `SALES_RETURN_REVERSAL` for SR-2026-2027-000002 dated 2026-08-19 |
| D-STK-10 | Low | **Batch and serial audit rows are bare `CREATE` / `UPDATE` / `DELETE` with empty `after_data`**, unlike every other module's `module.action` naming, so an exact-match filter for `batch.created` finds nothing; and `docs/BATCH_SERIAL_EXPIRY_ARCHITECTURE.md` still lists quantity columns on `batches` that the model no longer has, while the `InventoryTransactionType` docstring says counts and write-offs are unbuilt. | Live (the rows); Code (the docs) |

### Selling -- found writing the Selling data trail, 2026-09-19

Full write-ups: the PR that added `docs/DATA_TRAIL_BY_OPERATION.md` §11, and
the section itself. Live rows are the four selling fixture stores
(`fx_t0916h2j3_s`, `fx_t0916d751_s`, `fx_t09164wau_s`, `fx_t0916z0ph_s`) and
WHOLE01. Checked and clean, the Buying twins that do **not** recur: an order,
quotation or return cannot be created past DRAFT; a sales return cannot be
closed from DRAFT; cancelling a completed return reverses its stock, both
journals and the receivable; a return with no price takes the source line's.

| Id | Severity | Summary | Evidence |
| --- | --- | --- | --- |
| D-SELL-35 | Medium | **Editing a sales order that got free shipping may lose the charge originally asked**: the order editor stores only the charge after the waiver (the order twin of D-SELL-34). Found reading the code. | Code |
| D-SELL-33 | Low | Editing an existing draft bill that bills an order does not re-offer the serial picker (follow-on to D-STK-15, #489). | Code |
| D-SELL-22 | Low | **Closing an order accepts a DRAFT and leaves its claims** -- `close_order` refuses only CANCELLED and CLOSED and never calls `_release_promotions` (`sales_order_service.py:797-844`), so a closed draft keeps PENDING rows the redemption report lists for ever; PENDING rows soft-deleted on re-save get no `deleted_at` (`redemption_service.py`, `stage`). | Live: WHOLE01's soft-deleted PENDING rows; Code |
| D-SELL-23 | Low | **A credit note's trail stops at CREATED**: only create records a lifecycle event -- approve, cancel and edit write none (`backend/app/credit_note/services/credit_note_service.py:248-460`); its receivable row carries no `reference_type` or `reference_id` (`:353-372`), nor does a loyalty redemption's (`loyalty_service.py:425`); its lines have no `created_by`. | Live: `fx_t0916h2j3_s` CN-…-000001 APPROVED with only CREATED; WHOLE01's 12 notes likewise; 12 CREDIT_NOTE and 8 LOYALTY receivable rows with no reference type |
| D-SELL-24 | Low | **Audit and lifecycle names off the pattern:** the credit policy and the sales stages are audited as bare `CREATE` / `UPDATE` with `entity_type` `CreditControlSettings` / `SalesWorkflowSettings` (`backend/app/customers/services/credit_control.py:204-207`; `backend/app/sales_order/services/workflow_settings_service.py`, `update_settings`) -- the D-STK-10 twin; proforma lifecycle actions are `PROFORMA.CREATED` / `.ISSUED` / `.CANCELLED` (`proforma_service.py:242`, `350`, `408`). | Live: `fx_t09164wau_s`, `fx_t0916h2j3_s` |
| D-SELL-25 | Low | **`sales_orders.outstanding_balance_snapshot` records the customer's opening balance**, not what they owe (`sales_order_service.py:368`, `469`). | Live: every order in every store reads 0.00; `fx_t0916h2j3_s` SO-…-000005 was raised while Vijaya owed 483.21; WHOLE01 SO-2026-2027-000020 0.00 against 8,068.19 |
| D-SELL-26 | Low | **The order approval's credit warning is computed and discarded** -- `self._credit_assessment` is assigned and never read, though the comment says WARN "records the assessment on the order" (`sales_order_service.py:543-546`); neither stored nor returned, so an API client or the automatic chain never sees it. | Code |
| D-SELL-27 | Low | **Reversal and redemption dates and references disagree:** a redemption's journal is dated today in UTC (`loyalty_service.py:404`); a credit note's `-REV` is dated the note's date while receipts', returns' and invoices' are the first of the period (D-BUY-4); the TCS reversal's receivable row reuses `TCS-RC-…` with no `-REV` and no remarks (`backend/app/tcs/services/tcs_service.py:387-393`). | Live: `fx_t09164wau_s` `LOY-RED-SI-…` 2026-09-15 for a 2026-09-16 bill; `fx_t0916h2j3_s` and WHOLE01's TCS reversals |
| D-SELL-28 | Low | **Quotation and proforma small ones:** a DECLINED quotation (a terminal state) can still be cancelled (`quotation_service.py:545-548`); issuing a cancelled proforma says "… has already been issued." (`proforma_service.py:336-337`); a quotation has no coupon field, so a coupon-only offer can never be quoted. | Code |

### Finance -- found writing the Finance data trail, 2026-09-19

Full write-ups: the PR that added `docs/DATA_TRAIL_BY_OPERATION.md` §12, and
the section itself. Live rows are the `ready-firm` store TC-FIN-001, 003 and
007 were walked in (`fx_t0916hkkx_r`), the selling stores (`fx_t0916d751_s`,
`fx_t0919sosg_s`, `fx_t0919djcv_s`), TEST01 and WHOLE01, read with SELECT
only. Checked and clean: no period can be posted into while CLOSED or LOCKED
(create, post and every document approval check it; a reversal goes to the
period open on its own day); every period in every store balances and every
stored balance chains to the one before it, back-dated postings included; the
trial balance, P&L and balance sheet agree in every store checked; a receipt,
payment or refund cannot be written without its journal; no rounding gap
between an invoice's two-decimal total and its receivable row in any store.

| Id | Severity | Summary | Evidence |
| --- | --- | --- | --- |
| D-FIN-20 | Low | The desktop account picker for hand journals still lists the sub-ledger accounts the server now refuses by name (follow-on to D-FIN-11, #504). | Code |
| D-FIN-21 | Low | An ageing `as_of` a past day leaves out an invoice cancelled after that day, because a cancellation carries no timestamp (follow-on to D-FIN-10, #501). | Code |
| D-FIN-13 | Low | **Finance audit rows do not say what changed.** `finance.ledger_account.updated` records `name` and `is_active` on both sides (`finance_service.py:493`, `516-525`), `finance.accounting_period.updated` `status` and `name`, `finance.financial_year.updated` `name` and `is_active`; `finance.cost_center.updated` / `.profit_center.updated` carry no `before_data`; the 24 mappings written by Open the books have no audit row (`control_accounts.py:205-260`). | Live: `fx_t0916hkkx_r` two `finance.ledger_account.updated` rows for 5000 reading `{"name": "Purchases", "is_active": true}` on both sides while `requires_cost_center` was ticked and unticked (version 1 → 3); WHOLE01 four more |
| D-FIN-14 | Low | **The finance reports accept another firm's period.** All four read the period with `session.get` and no firm check (`general_ledger_service.py:216`, `308`, `452`, `508`), so in the shared store one firm's report on the other's period answers with its own figures on the other's calendar instead of "not found". | Code |
| D-FIN-15 | Low | **Small ones:** a draft journal cannot be edited, deleted or rejected -- `JournalEntryUpdate` is declared and unrouted, REJECTED is never written (`backend/app/finance/schemas/__init__.py:400-405`); a year with periods can never be deleted, since no endpoint deletes a period (`finance_service.py:159-188`); a reversal with a chosen period silently moves a date outside it to the period's first day (`journal_engine.py:254-256`); a group's parent can change to another type or into a cycle (`finance_service.py:374-378`); journal and voucher types are checked for the firm but not `is_active`, and documents post under whichever sorts first (`journal_engine.py:600-616`; `document_posting.py:206-219`); periods close in any order, so a closed month's stored balances still move when an earlier open one is posted into (`journal_engine.py:480-528`). | Code |
| D-FIN-16 | Low | **`verify_sample_data.py` does not separate firms in the shared store** -- its stock, receivable and control-account sums carry no `firm_id` (`backend/scripts/verify_sample_data.py:163-177`, `179-202`, `306-343`), so MEDI01's and FOOD01's differences are added together and can cancel. It checks nothing about payables, GRNI, TCS or loyalty payable. | Code |
| D-FIN-17 | Low | **A statement's reversal row carries the original's date** (`reverse_receivable_transaction`, `customer_service.py:935`) while its journal carries the cancel date, so a statement already sent for a month changes when something in it is cancelled later, and never agrees with 1100 at that month end. | Live: WHOLE01 SR-2026-2027-000002-REV receivable row dated 2026-08-19, cancelled 2026-09-13; `fx_t0916h2j3_s` RC-2026-2027-000001-REV dated 2026-09-16 beside its journal of 2026-09-01 |
| D-FIN-18 | Low | **The trial balance's Total row is not the total of the columns above it.** The Debit and Credit columns are the period's movement; the Total and the Balanced chip are the closing balances split by side (`general_ledger_service.py:73-99`; `desktop/lib/ui/finance/trial_balance_page.dart:239-270`). | Live rows: `fx_t0916d751_s` September -- the Debit column sums to 8,468.75, the Total reads 7,165.54 |

### Compliance -- found writing the Compliance data trail, 2026-09-19

Full write-ups: the PR that added `docs/DATA_TRAIL_BY_OPERATION.md` §13, and
the section itself. Live rows are a `compliance-firm` store built for the pass
and driven against the running backend (`fx_t0919l8ca_g`), the two
`compliance-firm` stores of 2026-09-16 (`fx_t0916irn8_g`, `fx_t09167ru4_g`),
the selling stores, WHOLE01, the shared store and ELEC01, read with SELECT
only. Checked and clean: no registration or e-way bill in any store is LIVE or
unmarked, and `mode` is NOT NULL with no default in every store checked;
`portal_for("LIVE")` raises; `simulate` never commits (the endpoint does);
rules are evaluated `priority`, `code`, `version_number` desc on NOT NULL
columns and evaluation stops at the first match (the trace of a matched run
lists four of six rules); the profile resolver ranks NULL `effective_from`
explicitly; no module reads the local clock; TCS is charged on the receipt,
only on the excess over the threshold, summed from receipts per the firm's own
year, posted to 2500 (WHOLE01's 2500 equals its collections, 648.24); every
e-invoice, GSTR and TCS read is scoped to the firm in context; GSTR-3B is summed
from the documents, not from GSTR-1.

| Id | Severity | Summary | Evidence |
| --- | --- | --- | --- |
| D-CMP-14 | High | **Interstate purchases are still charged CGST+SGST**: the GST template has no inward interstate rule, so the D-CMP-1 fix covers sales only. Needs template rules and a data migration for existing firms. | Code |
| D-CMP-15 | Medium | The printed sales invoice's place of supply is the billing-address state name, even for a registered buyer whose GSTIN is in another state. | Code |
| D-CMP-16 | Medium | Reversing or back-dating a receipt does not re-charge TCS on receipts already collected after it. | Code |
| D-CMP-17 | Low | The GSTR-1 HSN summary is not netted of credit notes and returns. | Code |
| D-CMP-18 | Low | A bill cancelled after its month's GSTR-1 was due credits back only its taxed lines in the month of cancellation, so 3B 3.1(c) is not reduced by its nil-rated or exempt lines (follow-on to D-CMP-10/11). | Code |
| D-CMP-13 | Low | **Small ones:** every execution log reads `execution_mode` SIMULATION, a document's lines included, and carries no document or line id (`tax_rule_service.py:606`); the template's `EXPORT_ZERO` and `PURCHASE_INPUT_CREDIT` name transaction types no document sends (`gst_template.py:284`, `316`), and its interstate rules name the LOCAL profile by id, so a rate change would unhook them; the register and withdraw responses carry an empty `invoice_number` and `customer_name` (`backend/app/einvoice/api/router.py:158-206`); the screen offers Raise bill only when an invoice has no bill row, so a withdrawn bill is raised again through the API alone; the payload sends item tax at four decimals, no round-off or header charges in `ValDtls`, `SupTyp` always B2B and `Pos` from the buyer's GSTIN (`payload.py:190-257`); a credit note's rate is its first line's (`gstr_service.py:472`); a receipt paying the TCS a buyer owes is itself charged TCS. | Code |

### Configuration -- found writing the Configuration data trail, 2026-09-19

Full write-ups: the PR that added `docs/DATA_TRAIL_BY_OPERATION.md` §14, and
the section itself. Live rows are a `config-firm` store built for the pass and
driven against the running backend (`fx_t09193ugd_r`; the backend was the
build before #500 and stopped at about 07:08 IST, so the loyalty rate change
was not driven), and every other store on the local server read with SELECT
only. Checked and clean: attribute values live in typed columns, never JSON;
a save without `attributes` leaves them alone and an empty list clears them;
unit values in the shared store are kept per firm; the conversion-rule
resolver ranks the product's own rule over the firm-wide one explicitly, not
by NULL order; a yearly restart whose number hides the year is refused when
the rule is saved; a preview issues nothing; profile assignment and readiness
open the named firm's store and read platform tables through the platform;
no configuration read inserts a settings row except preferences and the sales
hierarchy; no module in scope reads the local clock; every geography unique
index is partial on both databases.

| Id | Severity | Summary | Evidence |
| --- | --- | --- | --- |
| D-CFG-22 | Low | The demo seeder writes numbering series directly with `PURCHASE/` and `SALES/` prefixes shared across document types, which the D-CFG-8 check would refuse. | Code |
| D-CFG-23 | Low | Industry templates and profile UOM defaults still write no audit row, and existing null-firm / empty audit rows are not backfilled (follow-on to D-CFG-13). | Code |
| D-CFG-18 | Low | **Numbering and document-framework small ones:** `PUT /numbering-rules/{id}` accepts `next_sequence`, `last_scope_signature` and `document_type_id`, which the counter then ignores or which move a rule under another type (`document_framework_service.py:439-483`); the lifecycle state and type rows (`allows_edit`, `is_terminal`, `allows_print`, `transition_rules`, the type's `is_active`) are read by no module; a rule may name another firm's document type in the shared store (`create_numbering_rule` does not look it up); the print-template path's `document_type` is not checked against the types, and the desktop's Print settings omits `header_note`, so each save clears one set through the API (`desktop/lib/models/print_template.dart:88-104`; `backend/app/document_framework/services/print_template_service.py:47-87`); deleting a document type the modules use restarts its series as D-CFG-7 does. | Live: `fx_t09193ugd_r` `next_sequence` 3 accepted on a series at 51 -- the rule read 3, the preview `…000051`, the next receipt `…000051`, the rule then 52; the rest Code |
| D-CFG-19 | Low | **Three answers to "which profile is this firm on".** `/active-features` and `/active-modules` use `_resolved_profile_id` (`framework_service.py:872-900`), which falls back to *any* ACTIVE profile when a store has no default, while the gate enforces nothing and custom fields apply none (`backend/app/business/gating.py:147-177`; `attribute_service.py:448-465`) -- the "one rule, both paths" the framework doc promises; and both endpoints read the assignment of the firm named by `?firm_id=` in the store `X-Firm-ID` opens (`backend/app/business/api/router.py:655-735`). Clearing `is_default` on the default profile is enough to part them. | Live: from `fx_t09193ugd_r`, `/active-features?firm_id=<WHOLE01>` answered `ATTACHMENTS`, `BARCODE` (GENERIC); WHOLE01's own store answers six. Every store has a default today |
| D-CFG-20 | Low | **Custom fields are read one record at a time on every list.** Customers, vendors, products, warehouses and units build each row's `attributes` with its own query (`backend/app/customers/api/router.py:174`, `backend/app/vendors/api/router.py:176`, `backend/app/products/api/router.py:130`, `backend/app/branches/api/router.py:615`, `backend/app/uom/api/router.py:81`); `AttributeService.values_for_many` exists and nothing calls it. | Code |
| D-CFG-21 | Low | **Settings, units and places -- small ones:** `user_preferences.updated` / `.reset` carry no before, after or firm and are written on every screen change, and `GET /me/preferences` inserts with no audit (the §3 open question); the sales-hierarchy read inserts and its `PUT` resets omitted flags; readiness counts an inactive profile assignment and memberships of deleted users as done; an explicit `null` on a NOT NULL field of the loyalty settings, preferences, a unit or a conversion rule answers 409 "conflicts with existing data" instead of 422; the Set up panel's profile assignment clears the Profile Assignment screen's `notes`; a unit used only on document lines can be deleted; the profile-wide default units reach one store though the endpoint and `docs/UOM_FRAMEWORK.md` say every firm on the profile; the loyalty seeder writes a no-change `loyalty.settings_changed` on every run; geography edits are refused for a code another parent uses, reset omitted fields through the API and still offer inactive places; full (non-partial) unique keys on units, groups and packaging types refuse a soft-deleted code for ever; the unit catalogue is 19 rows in some stores and 36 in others. | Live: 521 empty `user_preferences.updated`; TEST02 readiness counts 12 members with no live user; 46 no-change loyalty rows; 19 vs 36 units; the rest Code |

### Identity, firms and audit -- found writing the data trail, 2026-09-19

Full write-ups: the PR that added `docs/DATA_TRAIL_BY_OPERATION.md` §15, and
the section itself. Live rows are `platform`, every store's `audit_logs`
trigger (58 schemas in `agency_platform` and `electrolink_ops`), and three
fixture runs driven against the running backend -- `platform-operator`
`t0919tkcy`, `platform-admin` `t0919wwzu`, `two-firm-user` `t0919subq` -- plus
a probe firm `IDNPROBE1`, created and deleted unprovisioned. **D-IDN-1 and
D-IDN-2 are privilege escalations and were deliberately not driven**; the
evidence is the code path. Checked and clean: no firm-owned service reads
`users`, `firms` or `user_firms` on a tenant session (delivery note, purchase,
sales order, territory and print support all go through `platform_reader()`
or the platform session); every firm route and readiness open the named
firm's store with `firm_store_session`; no private firm-scope resolver on a
firm-owned route; every enforced code is seeded; the reserved role codes are
refused case-folded and `RoleUpdate` cannot rename a code; email, firm code,
GST, PAN and the active primary are unique among live rows by partial index
as well as by the service; no two firms share a database/schema pair; routing
is refused on update; a deactivated, expired or deleted user is refused on
the next request and the next refresh, and holds no live refresh token;
every change that alters a token's grants bumps `authorization_version`; no
module in scope reads the local clock; `/me/firms` ranks with an explicit
`case`, not NULL order; the append-only trigger and its function are in every
store; retention is opt-in by design and has not run here.

| Id | Severity | Summary | Evidence |
| --- | --- | --- | --- |
| D-IDN-1 | High | **A `PLATFORM` operator can grant themselves firm roles, undoing the narrowing.** The operator holds `ROLE_ASSIGN`, and `PUT /users/{id}/roles` from any platform caller writes the global tier of **any** user, their own row included (`backend/app/identity/api/router.py:122-124`, `609-621`; `backend/app/identity/services/identity_service.py:1623-1633`). `_issue_tokens` gives a `PLATFORM` administrator's memberships every global-tier seeded firm role (`identity_service.py:2098-2133`), so an operator who assigns themselves `FIRM_ADMIN` holds it in every firm they are a member of from their next sign-in -- the books TC-TIER-002/003 say they are refused. The three narrowing points are each correct; nothing stops the operator writing the rows they read. | Code -- not driven (an escalation); the `t0919tkcy` token carries TEST01 and TEST02 with 0 codes each, so one global row is all it lacks |
| D-IDN-2 | High | **Any platform administrator can take over any other, including a wider one.** `POST /users/{id}/password` is `require_platform_admin()` of either reach and `reset_password` never looks at the target's designation (`backend/app/identity/api/router.py:582-606`; `identity_service.py:574-626`), so a `PLATFORM` operator can set an `ALL_FIRMS` administrator's password with no forced change and sign in as them -- every firm's books. `PATCH /users/{id}` likewise lets them rename, deactivate or expire one (`identity_service.py:513-548`; `_get_user` excludes platform administrators only for a firm caller). Only deletion checks the designation. | Code -- not driven (an escalation) |
| D-IDN-3 | High | **The global role tier takes grants that belonged to one firm.** A platform administrator's **Hire like this person** with no firm named copies every role the source holds in any tier (`_roles_held_by`, `identity_service.py:943-974`) and writes them all as global rows (`:921-923`), so a role the source holds in one firm applies to the clone in every firm they are copied into. The global path of `set_user_roles` checks only that the role ids exist (`:1623-1633`), so it also accepts another firm's own custom role, and `apply_user_template` with no firm named applies any firm's template to the global tier (`:1203-1213`, `1256-1285`). | Live: `t0919subq` -- the source holds SALES_EXECUTIVE in TEST01 only; its clone `t0919subq.clone` holds SALES_EXECUTIVE globally, in TEST01 and TEST02. The custom-role half Code (no such row today) |
| D-IDN-4 | High | **A dedicated firm can be pointed at a schema another store owns, and provisioning then drops the platform tables from it.** `_assert_storage_unclaimed` compares only with other firms' mappings (`backend/app/firms/services/firm_service.py:420-446`); SHARED firms record no schema and `platform` is nobody's mapping, so a SCHEMA or DATABASE firm may name `firm_shared`, `platform` or `public`, and `schema_name` has no pattern (`backend/app/firms/schemas/firm.py`). **Provision** migrates that schema and runs `prune_platform_objects` on it -- `DROP TABLE ... CASCADE` of `users`, `roles`, `user_firms`, `firms` and the rest (`backend/app/core/tenancy/lifecycle.py:51-86`, `103-157`): on `platform`, the identity store and the registry. On `firm_shared` it puts a "dedicated" firm among four others. | Live: `IDNPROBE1`, SCHEMA on `agency_platform/firm_shared`, accepted 201 at 11:51:22 IST, deleted a second later, never provisioned; the prune Code |
| D-IDN-5 | Medium | **Identity administration the firm's trail cannot show, and rows that name no change.** `user.firms_set` never carries a firm (`identity_service.py:1758-1764`); `user.created`, `.updated`, `.deleted`, `.roles_set` and `role.*` carry the **caller's** scope, null for every platform administrator, whatever firm the person or role belongs to; `user.restored` and `user.password_reset` carry none; a clone's copied memberships write no row (`:976-1006`). The merged read filters the platform store on `firm_id` (`backend/app/common/audit/services/reader.py:145`), so none of these reach the firm's screen. And `user.firms_set`, `user.roles_set`, `user.firm_roles_set`, `role.*`, `role.permissions_set` and `user.deleted` hold no data; `user.updated` a before and no after; `firm.updated` only name, code and active flag. | Live: 1,121 `user.firms_set` on the platform, **0** on TEST01's screen; `user.created` 2 of 290; the clone's three rows on no firm's trail; five of six `role.deleted` for TEST01's own roles with no firm |
| D-IDN-6 | Medium | **A `PLATFORM` operator cannot staff a firm -- except by cloning.** `_firms_the_caller_may_staff` is the firms where the caller holds `USER_CREATE` in `firm_permissions` (`backend/app/identity/api/router.py:98-119`), which an operator never does, so `PUT /users/{id}/firms` and `PUT /users/{id}/firms/{firm}/roles` refuse every firm and `GET /users/{id}/firms` answers nothing; User-Firm Assignments is blank for the tier whose job TC-TIER-001 says is creating firms and their people. `clone_user` with no firm named copies memberships with no reach check (`identity_service.py:926-927`). | Live: `t0919tkcy` -- `GET /users/{clone}/firms` `[]` for a person in two firms; re-saving the clone's two memberships 422 "You can only assign firms you administer." |
| D-IDN-7 | Medium | **The identity routes take their firm from the raw `X-Firm-ID`.** `_firm_scope` returns `principal.firm_id` -- the header, checked against nothing (`backend/app/identity/api/router.py:122-124`; `backend/app/core/security/authorization.py:123`, `236-246`) -- a private firm-scope resolver. A firm administrator is held back only because their codes sit in one firm's `firm_permissions`; anybody holding a code in the **global** claim -- a global custom role, a platform role such as `SYSTEM_AUDITOR` -- lists a firm's people, creates users into it and sets its roles by naming it, with no membership. Firm-owned routes check the membership in `app/common/scope.py`. | Code -- latent: 0 global custom-role rows and 0 platform-role holders today |
| D-IDN-8 | Medium | **Hire like this person is three commits.** `clone_user` calls `create_user`, which commits, then `set_user_roles`, which commits, then copies memberships and audits (`identity_service.py:906-941`) -- "a chain of committing services is not a transaction". A failure after the first leaves an account holding the address with no roles or firms, and the retry is refused 409. | Live: the `t0919subq` clone's rows committed at 11:50:39.660, .846 and .943 IST; the failure Code |
| D-IDN-9 | Low | **A role code is unique across every firm, for ever.** `create_role` checks every role, deleted or not, in any firm (`identity_service.py:753-754`), and `UQ_roles_code` is a plain unique index -- so a deleted role's code can never be used again, and the 409 "A role with this code already exists." tells one firm another has a role by that name. | Live: `UQ_roles_code` on `platform.roles (code)`, no `WHERE`; the deleted fixture roles keep their codes |
| D-IDN-10 | Low | **Identity and firms -- small ones:** eleven seeded codes are enforced by no route -- `FIRM_CREATE`, `FIRM_UPDATE`, `FIRM_DELETE`, `FIRM_ACTIVATE`, `FIRM_DEACTIVATE`, `PERMISSION_CREATE`, `PERMISSION_UPDATE`, `PERMISSION_DELETE`, `USER_RESET_PASSWORD`, `USER_LOCK`, `USER_UNLOCK` -- so granting one grants nothing; `GET /users/{id}/global-roles` takes `ROLE_VIEW` and no scope, answering any user's global roles to any firm administrator (`backend/app/identity/api/router.py:676-697`); `/firm-members` lists members whose account is switched off (`backend/app/common/firm_metadata.py:164-174`); `_assert_exclusive_firm_user` counts only **active** memberships elsewhere, so a firm administrator can edit or delete platform-wide somebody switched off in another firm (`identity_service.py:2491-2505`); refresh-token reuse detection fires on any revoked token -- after sign-out, or two refreshes racing -- and signs the person out everywhere (`:253-256`, `2162-2189`); sign-out leaves the access token good for its 15 minutes; firm `PUT` is a full replacement, so an omitted `is_active` turns a switched-off firm back on, omitted GST and PAN are cleared, and `status` is free text read by nothing (`firm_service.py:145-187`, `335`); there is no firm restore; `user_templates` / `user_template_roles` are not in `_PLATFORM_TABLES`, so 55 firm schemas keep a copy of the 11 seeded templates nothing reads; `docs/ACCESS_CONTROL_FRAMEWORK.md` Part 4 says a global custom role lands in `firm_permissions` as well, and `_issue_tokens` puts only seeded firm roles there; `GET /permissions/{id}` is platform-only while the list takes `PERMISSION_VIEW`. | Live: one switched-off member listed; 55 template copies; the rest Code |

### Masters -- found writing the Masters data trail, 2026-09-19

Full write-ups: the PR that added `docs/DATA_TRAIL_BY_OPERATION.md` §16, and the
section itself. Live rows are six fixture runs driven against the running
backend -- `stock-ready` `t0919p81v`, `product-master` `t09199zi5`,
`customer-master` `t09193238`, `loyalty-viewer` `t09191qg3`, `shared-pair`
`t0919r8y3` and `po-received` `t091964t5` -- plus every store on the local
server read with SELECT only (59 schemas in `agency_platform` and
`electrolink_ops`); nothing was written to the demo firms. Checked and clean:
a customer edit is partial and guards both child collections and the custom
fields on `model_fields_set`, and reads `is_default`-style flags with the row as
the fallback; the branch and warehouse edits do the same; the vendor's six
collections tell `None` from `[]`; the credit limit is refused to a sales
manager (D-CFG-17 holds); customers', vendors', branches' and warehouses'
imports stage and commit once; the bulk endpoints audit every row and apply the
single-row delete guard before committing; every master audit row carries its
firm and lands in the firm's own store; the default branch and warehouse flags
are maintained with the demotion flushed first; no master service reads a
platform table on the tenant session, reads the local clock, or lets NULL
ordering pick a row; every list endpoint declares its page bounds and every
literal path is declared above `/{id}`.

| Id | Severity | Summary | Evidence |
| --- | --- | --- | --- |
| D-MST-1 | High | **A product holding stock can be deleted, and the stock is then invisible and unmovable.** `delete_product` (`backend/app/products/services/product_service.py:247-264`) checks nothing -- not stock on hand, not a reservation, not a batch, not an open document -- while `stock_by_product` filters deleted products out (`backend/app/inventory/services/inventory_service.py:461`) and `_validate_references` refuses every movement of one (`:3769-3777`). So the quantity and its value stay on the books where no screen shows them and no adjustment can reach them. `bulk_delete` does the same, a hundred at a time. | Live: `t0919p81v`'s product deleted 204 holding 45 in MAIN and 5 in a bin; the write-off refused "Product does not belong to the active firm."; **TEST01 already holds 13 deleted products carrying 432 units and 27,160.00 of `product_valuations`**, deleted 2026-09-16, one with 2 reserved |
| D-MST-2 | High | **A customer's standing discount is writable with `CUSTOMER_UPDATE` alone**, so the role the credit limit was taken from (D-CFG-17) can give the same customer 100% off instead. `default_discount_percent` is an ordinary field on `CustomerWrite` and `_assert_may_change_credit_limit` guards the limit only (`backend/app/customers/services/customer_service.py:589-609`); `SALES_MANAGER` and `CUSTOMER_SUPPORT` both hold `CUSTOMER_UPDATE`, and the rate is the customer tier of the shared discount rule, above the price list and the segment (`backend/app/core/utils/pricing.py`). | Live: `t09191qg3` (SALES_MANAGER) refused 403 on the limit, accepted 200 on `default_discount_percent` 100, then raised SO-2026-2027-000011 -- two at 100.00 billed as **0.00**, `discount_source` `customer` |
| D-MST-3 | High | **Another firm's segment, vendor category and branch type are accepted in the shared store, and the segment's discount is then applied.** `customer_group_id`, `category_id`, `type_id`, `business_profile_id`, `branch_type_id` and `warehouse_type_id` are written straight through by the four master services, and the order and quotation resolvers read the segment by id checking only `is_active` -- not the firm, not `is_deleted` (`backend/app/sales_order/services/sales_order_service.py:1354-1361`; `backend/app/quotation/services/quotation_service.py:801-807`). A segment deleted a second earlier is accepted the same way. | Live: in `firm_shared`, TESTSH1's `T0919R8Y3-X1` put in TESTSH2's 50% segment and TESTSH1's SO-2026-2027-000001 priced at **50.00**, `discount_source` `customer_group`; `T0919R8Y3-V1` in TESTSH2's vendor category and `-B1` on TESTSH2's branch type; in TEST01, `T09193238-GRP` created into a retired segment |
| D-MST-4 | Medium | **The customer and vendor delete guards look at money and invoices, not at the documents in flight.** `_assert_account_is_square` counts balances and sales invoices (`customer_service.py:791-842`) and, for a vendor, bills, advances and supplier credits (`vendor_service.py:186-264`) -- so a customer with an approved order holding a reservation, or a supplier whose goods have been received and not billed, is deleted; every sales and purchase service then loads the party with `is_deleted.is_(False)`, so the documents can neither move nor be cleared. | Live: `t091964t5`'s vendor deleted 204 with GRN-…-000027 and -000028 COMPLETED and unbilled, GRNI (2300) holding 1,000.00 and the Payments outstanding list answering `[]`; `t09193238`'s customer deleted 204 with SO-2026-2027-000012 APPROVED and 1 unit reserved |
| D-MST-5 | Medium | **A product update dumps its whole write model.** `_product_values` has no `exclude_unset` (`product_service.py:1014-1019`) and `attributes` and `media` are applied unconditionally (`:229-232`), so every field the caller omits is written back at its schema default and the custom fields and images are cleared. The desktop sends every field, which is how the cost price goes: a caller without `PRODUCT_VIEW_COST_PRICE` is served `purchase_price: null` (`backend/app/products/api/router.py:403-409`) and `product_management_page.dart:2618` sends that null back. | Live: a PUT naming code, name and type left `T09199ZI5-PM` with no category, no `GST_18_LOCAL`, no units and no 100.00 selling price; the cost-price half Code (no seeded role holds `PRODUCT_UPDATE` without the cost code) |
| D-MST-6 | Medium | **A customer's status is not a control on the sales side.** Nothing in quotation, order, delivery note or invoice reads `customers.status`, though the delete refusal tells the user to "set the customer inactive to stop trading with them", and the purchase side does check its counterparty (`backend/app/purchase/services/purchase_service.py:1513`, `1547-1560`). A product's `status` is likewise unread on the sales side. | Live: with `T09193238-CM` INACTIVE, SO-2026-2027-000012 was created and approved |
| D-MST-7 | Medium | **A product's base unit and its tracking flags can change under stock.** `update_product` re-validates the unit ids exist and nothing else, so the base or inventory unit moves while `inventories.current_quantity` -- which is in base units -- is left as it is, and `track_serial` / `require_serial_on_issue` can go on over stock received without serials, which the D-STK-4 picker then refuses to dispatch. | Live: `T09193238-P` moved PIECE → BOX and gained `track_serial` with 50 on hand and 1 reserved, 200 |
| D-MST-8 | Medium | **A storage area holding stock can be deleted** -- the warehouse guard's missing twin. `delete_storage_node` checks only for live children (`backend/app/branches/services/branch_warehouse_service.py:772-788`) while stock is held per node (`inventories.storage_node_id`, the grain D-STK-13 counts at), and `_validate_references` then refuses to move it. The delete and the edit write no audit row either. | Live: `T0919P81V-BIN` deleted 204 holding 5, and the transfer back out refused "Storage node does not belong to the selected warehouse." |
| D-MST-9 | Medium | **The product import is the one import that commits per row.** All three formats end in `import_products_json`, which loops over `create_product` (`product_service.py:577-585`), and that commits -- so a file whose second row clashes answers 409 with the first row written, and the corrected file is then refused as a duplicate. Customers, vendors, branches and warehouses all stage and commit once. The CSV and XLSX readers also fill only seven columns and raise on a bad number or status rather than naming the row. | Live: a two-record JSON import whose second row reused `T09199ZI5-PM` answered 409 and left `T09199ZI5-IMP1` written |
| D-MST-10 | Medium | **Five seeded master codes are enforced by no route** (the D-IDN-10 twin, which listed eleven others): `VENDOR_MANAGE_BANK_DETAILS` and `VENDOR_VIEW_FINANCIAL_DETAILS` -- the scope is declared in `backend/app/vendors/api/router.py:78-80` and used nowhere -- so the account a payment is sent to is edited with `VENDOR_UPDATE` and read with `VENDOR_VIEW`, which the seeded `VIEWER` holds; and `PRODUCT_PRICING_MANAGE`, `PRODUCT_TAX_MANAGE`, `PRODUCT_ATTRIBUTE_MANAGE`, so a price, a tax group and the custom fields all ride on `PRODUCT_UPDATE`. Granting any of the five grants nothing. | Code: `grep -rn` over `app/` finds each only in `system_seed.py` (and the unused scope) |
| D-MST-11 | Low | **Masters -- small ones:** every master code is a plain unique index covering deleted rows, so a code is never released, and where the service's own check filters `is_deleted` (customer segments, product codes and barcodes, vendor categories and types) the refusal arrives as the bare 409 "The request conflicts with existing data. Please retry."; `UQ_products_firm_barcode_active` declares `postgresql_where` and no `sqlite_where` (`backend/app/products/models/product.py:81`), so the unit suite gets a stricter index than production; **nine master writes record no audit row at all** -- vendor categories and types, branch and warehouse types, a vendor duplicate, a product category created or edited, and a storage node edited or deleted -- and the four category/type edits silently **un-delete** a retired row (`vendor_service.py:514-537`, `590-611`; `branch_warehouse_service.py:873-896`, `936-959`); a product category can be moved under its own child and descendants are never repathed (`product_service.py:465-483`), while `delete_category` looks only at `category_id`, ignoring sub-category use and live children; `display_name` is recomputed from `name` on every vendor, branch and warehouse update that sends one, losing a custom display name; the small masters publish no `ETag` and take no `If-Match`; `bulk-category` and `bulk-profile` write their id with no check; a vendor duplicate is refused 409 the second time, because `-COPY` does not count up as the product's does; the product list loads every row of the firm and slices in Python (`product_service.py:82-113`); the product CSV export joins on commas without quoting (`:514-539`), so a name containing one shifts every column; `vendors.business_attributes` is a JSON blob beside the typed custom fields, written by the API and the demo seeder and read by nothing; and `/branch-warehouse/settings` still answers `stock_transfer_ready: false` though `/api/v1/inventory/transfers` has shipped. | Live: the 409 on a retired segment code; no `vendor_category.*`, `branch_type.*`, `product.category.created` or `warehouse.storage_node.deleted` row in any of the 59 stores; `T09199ZI5-PC` at path `T09199ZI5-PC/T09199ZI5-PCC/T09199ZI5-PC`; six of seven `firm_shared` vendors carrying a `business_attributes` blob; the rest Code |

### Territory, commission and targets -- found writing the data trail, 2026-09-19

Full write-ups: the PR that added `docs/DATA_TRAIL_BY_OPERATION.md` §17, and the
section itself. Live rows are one `commission-firm` fixture run driven against
the running backend, `t0919tkvs` (firm `T0919TKVS-T`, schema `fx_t0919tkvs_t`),
plus every store on the local server read with SELECT only (59 schemas in
`agency_platform` and `electrolink_ops`); nothing was written to the demo firms.
Checked and clean: a payout is snapshotted at accrual and nothing downstream
re-reads the report; `UQ_commission_payouts_period_active` is partial in both
dialects and the overlap read stands beside it; the accrual, the payment and the
reversal carry three distinct references (`…`, `-PAY`, `-REV`) and the reversal
mirrors the accrual exactly; a payout's `status` is refused on its update body
(422) and the commission rule and payout updates are partial
(`exclude_unset`, `slabs` guarded on omission); a rule with slabs ignores its
flat rate in `_earned`, and the desktop labels the box "Rate (unused)" and
shows no single rate for a ladder; a margin line with a NULL cost contributes
nothing; the governing rule is resolved per row on its own date and each rule's
subtotal is laddered separately; the Unassigned bucket earns nothing and is
never paid; a round's reorder releases only the stop numbers actually moving and
flushes before writing them; a blank route is judged on the document's own
date (an order dated after the window closed kept its territory and took no
route); the four bulk routes and the territory import stage and commit once;
customers on a round and outlets on a plan are checked for the firm; a
salesperson put on a round, or named on an order, note or invoice, is checked
for membership through the platform store; every name is read through
`FirmMetadataReader` or `platform_reader()`; nothing reads the local clock;
the call list ranks unplaced stops on an explicit `case`; every list declares
its page bounds; every audit row these modules write lands in the firm's own
store with its firm. Commission is measured on the document total, tax and
freight included -- an open question `docs/COMMISSION_FRAMEWORK.md` leaves with
the owner, recorded here as a decision and not as a defect.

| Id | Severity | Summary | Evidence |
| --- | --- | --- | --- |
| D-TER-1 | High | **A margin rule cannot be written: `measure` is accepted and dropped.** `CommissionRuleCreate` and `CommissionRuleUpdate` both declare it, `create_rule` and `update_rule` never assign it (`backend/app/commission/services/commission_service.py:200-247`, `249-328`), `rule_response` does not pass it (`:738-784`) and `_snapshot` leaves it off the audit row, so a MARGIN rule is stored and read back as VALUE and pays its rate on the whole sale price -- the number `docs/COMMISSION_FRAMEWORK.md` calls the worst this report could produce. The desktop has no control for it. The only MARGIN rows anywhere are the demo seeder's direct write to the column; the seeder's own `create_rule(measure=MARGIN)` for a fresh store loses it the same way (`backend/scripts/generate_transaction_history.py:1729-1741`), and the unit suite builds its margin rules on the model (`backend/tests/unit/test_margin_commission.py:100-110`), which is why it is green. | Live: a PUT of `{"measure": "MARGIN"}` answered 200 with `measure: VALUE`, a POST of a MARGIN rule 201 with VALUE, and the column reads VALUE for both; the four MARGIN rows that exist (WHOLE01 one, `firm_shared` two, ELEC01 one) are the seeder's in-place correction, not a service write |
| D-TER-2 | High | **Two targets over the same days count the same sales twice, and the bonus is paid on the total.** `_assert_free` compares `period_start` alone (`backend/app/sales_targets/services/sales_target_service.py:355-384`), so a quarterly target from the 1st is refused because the monthly one starts that day while a target from the 2nd, overlapping it entirely, is accepted; `_targets_met` then sums every overlapping target's amount and every one's achievement (`commission_service.py:1102-1148`), each measured over its own period. The monthly-beside-yearly arrangement the framework doc describes double-counts the same way. | Live: Bala sold 4,720.00 against a target of 8,000.00 -- `target_met: false`, commission 94.40; a second target of 1,000.00 from 2026-09-02 made it 9,440.00 against 9,000.00 -- `target_met: true`, commission **188.80** |
| D-TER-3 | High | **A credit note, a sales return and a refund take nothing off commission, the invoiced figure or a target.** `_invoiced` sums `grand_total` of APPROVED and CLOSED bills and `_collections` sums allocations of POSTED receipts (`commission_service.py:958-1000`, `1277-1311`); `SalesTargetService._invoiced` and `_collected` are the same two walks (`sales_target_service.py:259-302`). Neither joins `credit_notes`, `sales_returns` or a REFUND settlement, so a bill credited in full still earns its commission, still meets its target, and still triggers the target bonus. Only a reversed receipt drops out. | Live: CN-2026-2027-000001 for the whole of SI-2026-2027-000003 (4,720.00), APPROVED; Bala's achievement, collected, invoiced and commission all unchanged |
| D-TER-4 | High | **One person states the debt, raises it, approves it and pays it -- their own included.** `approve` and `pay` look at the status and at nothing else (`backend/app/commission/services/payout_service.py:352-404`, `406-475`): not at who accrued or approved the payout, and not at whose it is. The seeded `ACCOUNTANT` holds the whole commission group (`backend/app/identity/system_seed.py:436`) and `FIRM_ADMIN` holds it too, so `COMMISSION_PAY` being a separate code separates nobody who holds either role; `update_payout` puts no ceiling on a positive adjustment; and the payout row keeps no approver or payer. | Live: Bala, given `ACCOUNTANT` beside `SALES_EXECUTIVE`, accrued his own payout (94.40), adjusted it by +5,000.00 with the reason "because", approved it at 5,094.40 and paid it -- five audit rows with his own id as actor |
| D-TER-5 | Medium | **A payout is paid from whatever account, on whatever date, the caller names.** `pay` passes `money_account_id` and `paid_on` straight to `post_commission_payment` (`backend/app/finance/services/document_posting.py:1154-1218`), and `JournalEntryEngine._load_accounts` asks only that the account is this firm's -- so the credit leg lands on Trade Receivables, on Sales, or on Commission Payable itself, which marks the payout PAID and moves no money. The desktop offers cash and bank accounts only; receipts and payments resolve theirs from the CASH / BANK control account. `paid_on` may precede `accrued_on`, leaving 2400 in debit between the two. | Live: `COMM-202609-9b8f08c7-PAY` dated 2026-09-01 with legs `2400 5094.40/0.00, 2400 0.00/5094.40`, against an accrual dated 2026-09-30; in `fx_t0916zqbt_t` TC-INCENT-007's own payment is dated 2026-09-16 and its accrual 2026-09-30 |
| D-TER-6 | Medium | **Money that lands in a period after its payout was accrued is never commissioned.** `accrue` accepts a period that has not ended (`payout_service.py:144-236`; `accrued_on` defaults to `period_end`, so the journal is dated in the future), the payout then holds the whole period against a second accrual, and a PAID payout cannot be cancelled -- so whatever is collected between the accrual and the period's end belongs to no payout. The same hole opens backwards: `POST /receipts/{id}/allocate` adds the allocation to the original settlement and the report dates it by `settlement_date` (`commission_service.py:974-993`), so an advance applied to a bill months later is counted in the month the advance arrived. Any start and end are accepted as a period. | Live: September accrued on the 19th, `accrued_on` 2026-09-30, in both `commission-firm` stores; RC-2026-2027-000004 (118.00, 2026-08-15) applied to SI-2026-2027-000004 of 2026-09-19 reported as August's 118.00 collected and 4.72 earned; a payout for 2026-09-10 to 2026-10-15 accepted |
| D-TER-7 | Medium | **A territory edit that leaves out `route_profile` deletes the route.** `TerritoryUpdate` is `TerritoryCreate`, `update_territory` assigns every column from the body (`backend/app/sales/services/territory_service.py:1437-1490`) and `_upsert_route_profile` soft-deletes the profile when it is handed `None` (`:3773-3779`) -- so the node stops being a route, its beat plans stop running, a new plan is refused and documents stop being tagged with it, all from an omission. `update_beat_plan` is the same shape: every column from the body and `customer_stops` replaced, so leaving them out clears the plan's outlets (`:2144-2180`). Neither change is on the audit row. | Live: a PUT of `T0919TKVS-R-N2` without `route_profile` answered 200 with `route_profile: null`; its three plans then read "The route is not in force on this date." and a new plan was refused "… is not a route."; `sales_territory.updated` carries the same code and path on both sides |
| D-TER-8 | Medium | **A target edit that leaves out `salesman_id` makes it the firm's target.** `update_target` assigns every column from `SalesTargetWrite` (`sales_target_service.py:132-165`), whose defaults are `None`, `None`, MONTHLY, INVOICED and ACTIVE -- so an omitted person or territory is cleared, an omitted basis becomes INVOICED and the notes go. The person's `target_met` then reads null and their bonus stops. The audit row records the amount only. | Live: a PUT of Asha's target naming its dates and amount answered 200 with `salesman_id: null`; the achievement row read "Whole firm" at 10,620.00 and her `target_met` went from true to null |
| D-TER-9 | Medium | **A route the caller names is kept as sent.** `resolve_sales_scope` validates a named territory and a named salesperson and returns a named `route_id` untouched (`backend/app/sales/services/scope_resolution.py:85-91`); the order and invoice services then check only that the profile exists (`backend/app/sales_order/services/sales_order_service.py:2165-2173`; `backend/app/sales_invoice/services/sales_invoice_service.py:2985-2993`) -- not that it is this firm's, this customer's, this territory's, or in force on the document's date. The effective window CLAUDE.md calls enforced is enforced on a blank and on nothing else, and in the shared store the id may be another firm's. | Live: with S1 closed at 2026-06-30, SO-2026-2027-000005 (dated 2026-09-19) kept S1's profile and SO-2026-2027-000006 kept N2's for a customer on S1, while the blank SO-2026-2027-000004 took no route |
| D-TER-10 | Medium | **A shop cannot be put back on a round it was once primary on.** `set_customers` un-deletes the old assignment row and leaves `is_primary` as it was retired (`territory_service.py:1670-1674`); if the shop became primary elsewhere meanwhile the save trips `UQ_territory_customer_assignments_primary_active` and answers the bare 409, from the picker that sends no flag at all. `_has_primary_elsewhere` is asked only for a row being inserted. | Live: `T0919TKVS-C1` taken off N1, added to S1 (primary), and the save putting it back on N1 answered 409 "The operation violates uniqueness constraints." with nothing written |
| D-TER-11 | Medium | **Somebody who has left the firm is still derived on to new orders, and the order then cannot be delivered.** Deleting a user or ending a membership touches no firm store, so `territory_salesman_assignments` keeps them; `_derived_salesman` reads the row without asking `FirmMetadataReader` (`scope_resolution.py:167-190`), and the order service validates only what the caller sent, before the derivation (`sales_order_service.py:318-335`). The delivery note and the invoice do check, so the order saves, approves and reserves stock and its note is refused. The user delete looks at no round, rule or payout. | Live: with Asha deleted, SO-2026-2027-000008 was created and approved in her name and its delivery note answered 422 "Salesman is not an active member of this firm."; her assignments on N1 and S1 are still live |
| D-TER-12 | Medium | **A target set on a Territory or a Region achieves nothing.** `_scope_clauses` matches `sales_invoices.territory_id` exactly (`sales_target_service.py:304-318`), and a document carries the node its customer is assigned to -- the route -- so a target on any level above it reads 0 however much its routes sell. The achievement row for a territory target is labelled "Whole firm", and such a target decides nobody's bonus. | Live: a 5,000.00 target on `T0919TKVS-RGN` reads 0.00 achieved with 10,620.00 invoiced on the three routes beneath it |
| D-TER-13 | Medium | **Copying a subtree commits node by node, and finds the subtree by prefix.** `copy_hierarchy` calls `create_territory`, `set_customers` and `set_salesmen` with their default `commit=True` (`territory_service.py:2667-2769`), so a copy refused partway -- the one-route-per-salesperson setting, a node cap -- leaves the nodes before it written, where the import and the four bulk routes stage and commit once. The source nodes are selected with `path.ilike('<source path>%')` (`:2682`), which also takes a sibling whose code starts the same way (`T-N` and `T-N2`), and reads `_` in a code as a wildcard. | Code |
| D-TER-14 | Low | **A route is deleted from under its beat plans and targets, and edited while deleted.** `delete_territory` looks for children, customers and salespeople only (`territory_service.py:1492-1536`); `call_list` reads the plan's territory without `is_deleted` (`:2230-2234`), so the plan goes on being reported as running, and a target naming the node stays in the achievement report. `update_territory`, `update_beat_plan`, `bulk_status_change` and `bulk_move` all load with `include_deleted=True`, so a PUT renames a node that is in the bin and leaves it there. | Live: `T0919TKVS-R-X` deleted 204 with a live plan and a live target, `GET /call-lists?date=2026-09-21` still answering `T0919TKVS-BP-X` `occurs: true`, and a PUT renaming the deleted node to `-R-X2` |
| D-TER-15 | Low | **Ids nobody checks.** No store carries a foreign key from a rule, payout or target to a person (`users` is platform), and the services do not ask either: a commission rule and a sales target accept any `salesman_id` at all, a target any `territory_id` that exists in the store, and a quotation or a return any salesperson when the customer is on no territory (`_validated_salesman` returns the id as sent). `create_territory` takes any enabled `hierarchy_level_id` and any `route_type_id` without asking whose they are, which in the shared store may be another firm's. An unknown `product_id` on a rule reaches the key and answers the bare 409. | Live: an unknown salesman 201 on a rule (listed as "Former member"), 201 on a target and 201 on QT-2026-2027-000001, and 422 on an order; an unknown product 409. Code for the level and route type |
| D-TER-16 | Low | **Territory, commission and targets -- small ones:** `sales_territory.updated` reads `before.code` after the row was assigned, so a rename records the new code twice, and `.deleted`, `.restored`, `hierarchy.updated` and every `beat_plan.*` row carry nothing on either side; `customers_set` and `salesmen_set` record a count, so who joined or left a round is on no trail, and a target's rows record the amount alone; `delete_target` sets `is_deleted` and leaves `deleted_at` and `deleted_by` NULL; `UQ_sales_targets_scope_period` is a plain unique key over two nullable columns, so it holds nothing for any target with a blank scope and never releases a deleted one; a target's `status` is free text (`PAUSED` is accepted, and anything but `ACTIVE` simply vanishes from the report); the commission rule overlap guard is a read followed by an insert with no key behind it; `UQ_sales_territories_firm_code` and `UQ_sales_beat_plans_firm_code` cover deleted rows while the services' own checks do not, so a retired code is refused as the bare 409; a salesperson assignment's `is_primary` defaults to false, so a client that omits it demotes; `sales_territories.status` is read by nothing, so an INACTIVE round still tags a sale and is still called; `CommissionRuleCreate` requires a `percentage` even for a ladder or a per-unit rule. | Live (the audit rows, `PAUSED`, the indexes); Code |

### Found in manual testing and not yet fixed -- `docs/BACKLOG.md` §31

| Id | Severity | Summary | Evidence |
| --- | --- | --- | --- |
| BL-31.14 | Low | **What a saved sales document does not show:** the resolved discount percentage, the coupon, labels on the Allocate/Reverse icons, default invoice print copies. One decision inside it: whether a coupon should apply to an order converted from a quotation. (Its "Line 1" item was fixed in #398.) | As recorded 2026-09-13, not re-checked |
| BL-31.15 | Low | **Small gaps from sections 10-13:** no source-module filter on Journal Entries; Ctrl+K hides a failing search; the Loyalty page cannot show one customer's balance; promotion conditions print raw; targets cannot name a salesman from the desktop; two reports missing from the catalogue. | As recorded 2026-09-13, not re-checked |
| BL-31.16 | Low | **A refused password says only "does not meet the configured policy"** while the server sends which rule broke (`core/validation/common.py` passes `details`). | Server side re-checked 2026-09-18 |
| BL-31.17 | Low | **Audit trail filters match exactly**, so typing `user` finds nothing (`common/audit/services/reader.py`: `AuditLog.action == filters.action`). | Re-checked in code 2026-09-18 |

---

## Fixed

| Id | Fixed | Summary | PR | Guard |
| --- | --- | --- | --- | --- |
| D-STK-1 | 2026-09-19 | A count posted a batch line's difference to the product's untracked row (TEST01-fixture: batch stayed 10, a new row at -2). The adjustment now lands on the counted batch. | #452 | `test_a_batch_line_corrects_the_batch_it_counted` |
| D-STK-2 | 2026-09-19 | An order's reservation was taken on an expired batch while the in-date one stayed free. Reservation now skips expired stock like dispatch, judged on the order date, and names expired stock behind a back order. | #453 | three tests in `test_delivery_note_module.py` |
| D-STK-3 | 2026-09-19 | Posting a count committed line by line, so a failing line left earlier ones adjusted and journaled with the sheet DRAFT. The sheet posts in one transaction. | #454 | `test_a_sheet_whose_second_line_fails_writes_nothing` |
| D-STK-4 | 2026-09-19 | Serial numbers never moved. Owner decided: the storekeeper picks the serials on the delivery note; dispatch refuses until one serial per unit; serials go SOLD, and back to AVAILABLE on a sales return (new table `document_line_serials`, migration 20260919_0138). Verified live 2026-09-19: a short pick refused by name, a full pick dispatched with -0001/-0002 SOLD. | #465 | `test_serials_leave_with_the_goods.py` (23), `serial_picker_test.dart` |
| D-STK-11 | 2026-09-19 | A count with two or more differences could never be posted: each line's journal reused the count number, a unique reference (409). Owner decided: one journal per count, a pair of lines per difference. | #459 | `test_a_count_with_several_differences_posts_one_journal` |
| D-STK-13 | 2026-09-19 | A count summed every location but adjusted ROOT, so bin stock drifted (driven: ROOT 8, BIN-A 5 after counting 13 of 15). Decided by Claude, industry standard: count by location -- one line per product x batch x storage node (migration 20260919_0139). | #467 | four tests in `test_physical_count.py`, `physical_count_test.dart` |
| D-STK-14 | 2026-09-19 | A movement could name another product's batch (driven: an SHT adjustment accepted under AMX's batch). Every stock row now checks the batch belongs to its product and firm. | #461 | `test_a_stock_row_cannot_take_another_products_batch` |
| D-SELL-1 | 2026-09-19 | An approved invoice earned no loyalty points: the earn ran after the approval's commit and was lost. The points are staged in the approval's own transaction. | #458 | `test_the_points_a_bill_earns_are_saved_with_its_approval` |
| D-SELL-2 | 2026-09-19 | Cancelling an invoice kept the points it earned and their accrual. Owner decided: cancel takes back only the points still unspent, with their cost reversed; spent points stay spent. | #460 | `test_cancelling_a_bill_takes_its_points_and_their_accrual_back` and four more |
| D-SELL-3 | 2026-09-19 | An invoice could bill a delivery note that never dispatched. Only a dispatched note can be billed. | #462 | `test_a_note_whose_goods_never_left_cannot_be_billed` |
| D-SELL-4 | 2026-09-19 | Closing an approved note that never dispatched made it count as delivered and billable. Only a dispatched note can be closed. | #464 | `test_an_approved_note_that_never_dispatched_cannot_be_closed` |
| D-SELL-5 | 2026-09-19 | A hold stopped only a note's save. Edit, approve, dispatch and complete all refuse a held order; a held order's draft can still have its vehicle or remarks put right. | #466 | `test_a_note_approved_before_the_hold_does_not_ship` and two more |
| D-SELL-6 | 2026-09-19 | A return could be raised against a draft or undispatched note, or a draft or cancelled invoice. Only goods that left can come back. | #468 | `test_goods_that_never_left_cannot_come_back` and three more |
| D-SELL-7 | 2026-09-19 | The same goods could be returned twice, once through the note and once through the bill. Each route counts the other's returns; a bill whose goods came back through the note cannot be cancelled. | #469 | `test_goods_back_through_the_note_are_not_returned_again_through_the_bill` and two more |
| D-SELL-8 | 2026-09-19 | A receipt whose advance had been applied could not be reversed (driven: 422 "overtaken"). Reversal undoes every receivable row the settlement wrote, newest first. | #471 | `test_a_receipt_whose_advance_was_applied_reverses_in_full` |
| D-SELL-9 | 2026-09-19 | Converting a quotation skipped the promotion claim. Decided by Claude, industry standard (Odoo re-checks offers at confirmation): only what was typed on the quote carries over as typed; everything the pricing rule worked out is worked out again by the order on its own date, which stages the claim. | #482 | `test_a_converted_order_claims_the_offer_it_was_quoted`, `test_a_discount_typed_on_the_quotation_carries_over_as_typed` |
| D-SELL-10 | 2026-09-19 | Record Receipt offered a bill's full remainder after a return or credit note against it. Decided by Claude, mirrors D-BUY-6: a return raised on the bill's lines and an approved credit note come off the bill; a return raised on a note stays a credit on the account. One `credited_against()` serves receipts and the loyalty cap. | #474 | `test_returns_and_credit_notes_against_a_bill_come_off_what_it_owes`, `test_points_cannot_settle_what_a_credit_note_already_took_off` |
| D-SELL-11 | 2026-09-19 | An order could be cancelled after it shipped or was billed, and a note raised against a CLOSED order. Decided by Claude, industry standard (SAP B1, Odoo): a shipped order is closed, not cancelled; a note moves only while its order is open. | #475 | `test_an_order_that_has_shipped_cannot_be_cancelled` and three more; `document_status_gate_test.dart` |
| D-SELL-12 | 2026-09-19 | A sales invoice could be closed from DRAFT or CANCELLED. Only an approved invoice can be closed. | #463 | `test_only_an_approved_invoice_can_be_closed`, `test_a_cancelled_invoice_cannot_be_closed` |
| D-SELL-13 | 2026-09-19 | With the delivery-note stage automatic, saving a draft invoice dispatched the goods. Decided by Claude, industry standard: stock leaves at invoice approval, in the approval's transaction; cancelling the draft withdraws the note it raised. | #478 | `test_a_draft_bill_ships_nothing_until_it_is_approved` and two more |
| D-SELL-14 | 2026-09-19 | Converting a quotation was two commits, so a failure could leave an order behind a quotation still OPEN. It commits once. | #483 | `test_a_conversion_that_fails_half_way_leaves_nothing_behind` |
| D-SELL-15 | 2026-09-19 | An offer's claim limit was counted per row, so editing the offer reset it (driven: limit 1, two approvals). Claims count across the version group, which is locked whole. | #479 | `test_approval_counts_claims_on_every_version_of_the_offer`, coupon twin |
| D-SELL-16 | 2026-09-19 | A proforma left out freight and the order's charges (driven: order 375.81, proforma 300.41). It states what the order will bill; `other_charges` is derived and shown on the desktop. | #484 | `test_freight_and_the_order_s_charges_reach_the_total`; Dart test |
| D-SELL-17 | 2026-09-19 | Proformas and purchase invoices shared the `PI` prefix. Decided by Claude: proformas number as `PF`; a firm still on the old default moves once, with an audit row, and its counter carries on so nothing is renumbered. | #485 | `test_a_proforma_never_shares_the_purchase_invoice_s_prefix`, `test_a_firm_that_chooses_pi_afterwards_keeps_it` |
| D-SELL-18 | 2026-09-19 | Selling pickers could not reach older records (the D-BUY-11 twins). Billable documents, the return/credit-note source picker, the settlement party list and the proforma, quotation and direct-bill pickers read every page. | #487 | `desktop/test/selling_pickers_read_every_page_test.dart`; 2 backend tests |
| D-SELL-19 | 2026-09-19 | Spending goodwill points debited Loyalty Payable for value never accrued. Decided by Claude, industry standard (IFRS 15 / Odoo): every point held is a liability -- an adjustment posts Dr 5700 / Cr 2600 (and back) at the scheme's value. Goodwill granted before the fix is not back-filled. | #477 | `test_goodwill_points_are_owed_before_they_are_spent` |
| D-SELL-20 | 2026-09-19 | Points could not be spent on one bill twice (409, duplicate journal reference). Later redemptions take `-2`, `-3`. | #476 | `test_points_can_be_spent_on_one_bill_more_than_once` |
| D-SELL-21 | 2026-09-19 | A sales return worked its tax out again through today's rules (driven: a rate cut made a return of goods billed at 18% reverse 12%). Decided by Claude, GST credit-note rule: a return reverses the tax and components the bill charged, pro rata. | #481 | `test_a_return_reverses_the_tax_the_bill_charged`, `test_a_return_raised_on_the_bill_reverses_its_tax_too` |
| D-SELL-29 | 2026-09-19 | A return could lift its own quantity cap with `allow_over_return` in the request (driven: 50 returned against a note of 5, credited 4,832.10; purchase twin: 40 against a receipt of 4). Decided by Claude, industry standard: no over-return tolerance; the fields are gone and the cap always applies. | #470, #472 | `test_a_return_cannot_lift_its_own_cap` (sales and purchase) |
| D-STK-15 | 2026-09-19 | The desktop sales-invoice editor had no serial picker, so a serial-tracked bill could not be raised with the delivery-note stage automatic. The editor picks serials like the note editor. | #489 | backend test + 2 Dart tests |
| BL-31.13 | 2026-09-19 | The Stock Ledger's type filter offered types the server never writes and left out real ones. It offers exactly the written types, with labels; a guard fails on drift. | #455 | `test_stock_ledger_types_match_the_server.py`, `inventory_stale_filter_test.dart` |
| D-BUY-7 | 2026-09-19 | Billing part of a receipt line cleared the whole line's accrual (TEST01: a bill for 4 of 10 cleared 2300 by 1000.00 and booked 600.00 of false gain to 5400). Each bill now clears its share by quantity, and the bill that completes the receipt takes the rounding residual so the bills net exactly to what the receipt accrued. Existing over-cleared postings are not repaired. | #446 | `test_billing_part_of_a_receipt_clears_only_its_share_of_the_accrual`, `test_the_bill_that_completes_a_receipt_takes_the_rounding_residual` |
| D-BUY-1 | 2026-09-18 | Purchase approval could be skipped: a create accepted any status, so an order was born APPROVED (64 in `firm_shared`, from the seeder); the import's Status column did the same. A create is now always a draft; anything else is refused by name. | #438 | `test_an_order_cannot_be_created_past_draft` |
| D-BUY-2 | 2026-09-18 | Cancelling an approved bill left its journal posted; the receipt could then be cancelled too, debiting 2300 twice (TEST01: 2300 left at 600). Cancelling now reverses the bill's journal, and refuses while a payment or live return rests on it. | #440 | `test_cancelling_an_approved_bill_takes_its_journal_back`, `test_a_paid_bill_cannot_be_cancelled` |
| D-BUY-3 | 2026-09-18 | A purchase return (and a bill) with no price was valued at zero. Silence now takes the source line's price; a stated price, zero included, wins. Existing zero returns stay until a reseed. | #439 | `test_a_return_with_no_price_goes_back_at_the_source_lines_price`, `test_an_invoice_line_with_no_price_bills_at_the_source_lines_price` |
| D-BUY-4 | 2026-09-18 | A reversal was dated the 1st of the original's period. Owner decided: the cancel date, in the period open that day; with none open, the original's own date. Commission payouts and credit notes still pass their own dates. | #442 | `test_a_reversal_is_dated_the_day_it_happens` |
| D-BUY-5 | 2026-09-18 | A bill or a return could name a DRAFT or CANCELLED receipt (TEST01: a bill approved against a draft receipt). Only a completed receipt can be billed or returned. | #441 | `test_only_a_completed_receipt_can_be_billed_or_returned` |
| D-BUY-6 | 2026-09-18 | A completed return did not lower what the bill owed (TEST01: 708.00 after a 236.00 return). Owner decided: a return raised against the bill's own lines comes off that bill; others stay a supplier credit. | #447 | `test_goods_returned_against_a_bill_come_off_what_it_owes` |
| D-BUY-9 | 2026-09-18 | A DRAFT goods receipt could be closed. Only a completed receipt can be. | #443 | `test_a_draft_receipt_cannot_be_closed` |
| D-BUY-10 | 2026-09-18 | The return editor counted earlier returns from the newest 100 only. It reads every page. | #445 | `an earlier return past the first hundred still counts` (widget) |
| D-BUY-11 | 2026-09-18 | The bill and return receipt pickers offered the newest 100 completed receipts only. Both read every page. | #451 | `a receipt past the newest hundred can still be billed / returned` (widget) |
| D-BUY-12 | 2026-09-18 | A DRAFT or CANCELLED bill could be closed (TEST01: PI-2026-2027-000008). Only an approved bill can be. | #448 | `test_only_an_approved_bill_can_be_closed` |
| D-BUY-13 | 2026-09-18 | A DRAFT or APPROVED return could be closed (TEST01: PR-2026-2027-000004). Only a completed return can be. | #449 | `test_only_a_completed_return_can_be_closed` |
| BL-31.9 | 2026-09-18 | A supplier bill could not be raised from the desktop. Purchase Invoices has a New action over completed receipts. | #444 | `purchase_invoice_entry_test.dart`, `reachable_features_test.dart` |
| D-SETUP-1 | 2026-09-18 | Setup.exe's database step ran hidden and its exit code was never read, so a failed install reported success. | #432 | compiled; probe of `ExecAndCaptureOutput` |
| D-SETUP-2 | 2026-09-18 | Every Setup upgrade failed before its migrations: `ALTER ROLE ... WITH LOGIN` is refused to a non-superuser. | #432 | `tests/integration/test_database_bootstrap.py` |
| D-SETUP-3 | 2026-09-18 | The generated administrator password was lost in the hidden window. | #432 | -- |
| D-SETUP-4 | 2026-09-18 | `.env.example` shipped `AGENCY_APP_VERSION=0.1.0` and a live `REMOTE_A` profile into every customer's settings. | #433 | `tests/unit/test_env_template.py` |
| D-SETUP-5 | 2026-09-18 | The `-SkipStart` hint left out `-SkipSync`; an install folder with a space in its path never started the backend. | #434 | probe run; `tests/unit/test_cli_entry_point.py` |
| D-27-5 | 2026-09-16 | A firm whose people had been deleted could never be deleted. | #419 | `test_a_firm_whose_people_were_deleted_can_still_be_deleted` |
| D-2-1 | 2026-09-16 | A wrong password on an inactive or expired account named the account's state. | #418 | `test_a_wrong_password_never_names_the_state` |
| D-8-1 | 2026-09-16 | Dispatch drew an expired batch. | #418 | three tests in `test_inventory_foundation.py` |
| D-11-1 | 2026-09-16 | A new firm's territory hierarchy was invented on every read and never saved. | #418 | `test_a_new_firms_hierarchy_is_written_by_the_read_that_invents_it` |
| D-20-1 | 2026-09-16 | Asking for roles on somebody in no firm refused after the account was already made. | #418 | `save_refusal_keeps_the_record_test.dart` |
| D-27-1 to D-27-4 | 2026-09-16 | Product custom fields: an applicable field was never offered, a mandatory one blocked every product, an inert rule was enforced, a withdrawn choice could not be saved back. | #418 | `test_product_master.py`, `test_entity_attributes.py` |
| BL-31.3 | 2026-09-13 | Document views showed raw ids instead of product, unit, tax profile and customer names. | commits `43f949d`, `dccbf65`; #398 | `desktop/test/document_line_labels_test.dart` |
| BL-31.1, 31.2, 31.4-31.8, 31.10-31.12 | 2026-09-09 to 09-12 | Found in manual testing and fixed the same day; each is written up in `docs/BACKLOG.md` §31. | see §31 | see §31 |

Defects found and fixed during the manual-testing runs of 2026-09-13 to 09-15
(#336-#413) are recorded in each PR and in the plan-correction PRs for their
section; they were not given ids, and are not repeated here.

## Not a defect

| Id | Decided | Summary | Reason |
| --- | --- | --- | --- |
| D-STK-12 | 2026-09-19 | A manual adjustment can take stock below zero (driven: 3 adjusted by -100 read -97). | Decided by Claude, industry standard: kept. Negative stock through an adjustment is a deliberate, tested behaviour here (`test_adjustment_updates_projection_and_negative_stock_summary`) and is reported on the stock summary, as Tally and Odoo allow negative stock by default rather than blocking the correction. |
| D-FIN-1 | 2026-09-19 | A customer who owed money could be deleted, leaving 1100 holding the balance (driven: a customer with a 500.00 advance deleted, 204). Decided by Claude, industry standard: a customer or supplier who owes, holds an advance, or has an open or draft document cannot be deleted (set them inactive instead); delete no longer reverses the opening journal and restore re-posts one a pre-fix delete reversed. TEST01's five customers deleted on 2026-09-16 still carry 1,960.00 -- restoring them brings 1100 back into agreement; left for the owner. | #486 | `test_a_customer_who_owes_money_cannot_be_deleted`, `test_restoring_a_customer_whose_delete_reversed_its_balance_reposts_it`, `test_a_vendor_with_an_open_bill_cannot_be_deleted` |
| D-FIN-2 | 2026-09-19 | A document's journal could be reversed by hand while the document stood (driven: a receipt's journal reversed, the receipt then could not be). The hand-reverse endpoint refuses any journal with a `source_module` -- cancel or return the document instead; the desktop enables Reverse only for hand journals; the invoice-cancel lookup matches `reversal_of_id IS NULL`. | #488 | `test_a_documents_journal_cannot_be_reversed_by_hand`; `journal_entry_test.dart` |
| D-FIN-3 | 2026-09-19 | A locked financial year stopped nothing through the API (driven: a period of a locked year closed, reopened and posted into). A locked year takes no postings and its periods cannot be added, closed, reopened or edited; its audit rows record the lock. Decided by Claude: the lock stays irreversible, by design. | #490 | `test_a_locked_year_takes_no_postings_and_its_periods_stay_as_they_are` |
| D-FIN-4 | 2026-09-19 | The receivable endpoint moved INVOICE, TCS, LOYALTY, ADVANCE_APPLY and REFUND with no journal (driven: four types moved a balance, journal count unchanged). Each is refused and pointed at the endpoint that records it; CREDIT_NOTE, which posts a journal, stays. | #492 | `test_the_receivable_endpoint_refuses_what_another_module_records` |
| D-FIN-5 | 2026-09-19 | A reversal dated today in UTC could land the day before what it undid. A reversal is dated the later of today and the original's date; a typed date before the original is refused; an invoice cancel dates its receivable credit with the reversal. | #493 | `test_a_bill_cancelled_before_utc_catches_up_is_not_undone_the_day_before`, `test_a_reversal_cannot_be_asked_for_before_its_original` |
| D-FIN-6 | 2026-09-19 | Periods could overlap and be re-dated from under their journals, and posting picked an unordered period (driven: an overlapping period accepted; a period holding journals re-dated). Overlaps are refused; a period with journals keeps its dates; a year cannot move past its periods; the covering period is chosen by one fixed order. | #495 | `test_periods_do_not_overlap_and_keep_the_dates_their_journals_carry`, `test_posting_picks_one_period_the_same_way_every_time` |
| D-FIN-7 | 2026-09-19 | The balance sheet dropped CONTROL-type accounts (driven: 50.00 on a CONTROL TCS account left assets 50.00, liabilities 0). A CONTROL account goes where its mapped purpose puts it; unmapped or two-sided ones go by the sign of their balance. | #497 | `test_the_balance_sheet_carries_control_accounts_where_their_purpose_puts_them` |
| D-FIN-8 | 2026-09-19 | A mapped control account could be deactivated, and an inactive one mapped (driven: 1100 deactivated). Both refused, naming the purposes. | #498 | `test_a_mapped_account_stays_active_and_an_inactive_one_is_not_mapped` |
| D-FIN-9 | 2026-09-19 | A hand journal could take a document's reference, and the rolled-back number was re-issued on every retry (driven: three receipts refused 409 with the same number). Decided by Claude: hand journals keep to their own `JV-` space, which no numbering rule may use; a settlement steps over a number already held. | #500 | `test_a_hand_journal_cannot_take_a_documents_reference` and two more |
| D-FIN-10 | 2026-09-19 | Ageing ignored points spent on a bill, and `as_of` only moved the day count (driven: Record Receipt 383.21, ageing 483.21). One `settled_against()` serves both; `as_of` ages what was owed that day. | #501 | `test_the_ageing_ages_what_record_receipt_says_is_owed`, `test_as_of_ages_what_was_owed_on_that_day` |
| D-FIN-11 | 2026-09-19 | Hand journals could post to 1100, 1200 and 2300 (driven: all three POSTED). Decided by Claude, industry standard (reconcile-only accounts): receivable, payable, inventory, GRNI, commission and loyalty payable and every CONTROL account are left to their documents; tax, TCS, cash, bank, income, expense and equity stay open to hand journals. | #504 | `test_a_hand_journal_leaves_the_sub_ledger_accounts_to_their_documents` |
| D-FIN-12 | 2026-09-19 | A journal line's centre was checked for presence only: another firm's or an inactive one was accepted, and an unknown one read as a duplicate reference. Only the firm's own, live, active centres are taken, each refused by name. | #505 | `test_a_journal_line_takes_only_the_firms_own_live_centres` |
| D-BUY-14 | 2026-09-19 | A purchase bill or return could skip the goods receipt on the caller's say-so (driven: a bill of 1,180 approved with nothing received; a return of 4 took stock to -4). A second bypass needed no flag: a line naming `PURCHASE_ORDER` under a receipt header. Decided by Claude, industry standard (three-way match; nothing used direct billing): the flag is gone and a purchase-order source is refused on the header and on every line. | #499 | `test_a_bill_cannot_skip_the_receipt`, `test_a_return_cannot_skip_the_receipt` |
| D-SELL-30 | 2026-09-19 | A bill could lift its own quantity cap with `allow_over_invoice` (driven: 50 billed against a note of 5, 4,832.10 charged). Decided by Claude, industry standard: no over-invoicing tolerance; the fields are gone. | #494 | `test_a_bill_cannot_lift_its_own_cap`, `test_a_bill_cannot_charge_for_more_than_left` |
| D-SELL-31 | 2026-09-19 | A delivery note could lift its own cap and skip the reservation check with `allow_over_delivery` (driven: 42 shipped against 12 ordered). Decided by Claude: no firm-level tolerance exists, so delivery is capped at the order; a firm-level tolerance is a backlog item. | #496 | `test_a_note_cannot_lift_its_own_cap`, `test_a_note_cannot_ship_more_than_the_order` |
| D-SELL-32 | 2026-09-19 | A quotation ignored an offer's bill discount, free gift and free shipping (driven: quote 5,678.16, the same order 5,265.16). A quote is priced by the same offer evaluation as an order and claims nothing; conversion hands over only what was typed (migration 20260919_0140, applied to every store). Known gap: buy-X-get-Y free goods on a line carry over as typed. | #503 | 2 backend tests; 3 in `quotation_test.dart` |
| D-CMP-1 | 2026-09-19 | Every interstate sale was charged CGST+SGST (driven: a bill to a Karnataka GSTIN from Tamil Nadu). One place-of-supply helper compares the firm's (or GST-registered branch's) state with the buyer's GSTIN, else billing address; a differing state is `SALES_INTERSTATE` (IGST Act s.7, 8, 10(1)). Invoice, order, quotation, note and return all use it; an unknown state keeps the old behaviour. Purchase side: D-CMP-14. | #507 | 10 tests across 9 files; a guard stops a module naming its own type |
| D-CMP-2 | 2026-09-19 | Sales returns never reached GSTR-1 or 3B. A completed return is a credit note (CGST Act s.34): CDNR, a new CDNUR for an unregistered buyer on a B2CL bill, else netted off B2CS; 3B deducts it, cess included. | #508 | 4 backend tests; 6 desktop |
| D-CMP-3 | 2026-09-19 | Editing an active tax rule left the old version ACTIVE and matching. An edit retires or end-dates the old version in the same transaction; also fixed: editing a DRAFT rule with a condition answered 409. Stores already holding two ACTIVE versions are not repaired (SQL in the PR). | #509 | 4 tests |
| D-CMP-4 | 2026-09-19 | Returns and e-invoice rounded CGST and SGST separately (driven: journal 73.71, GSTR-1 36.86 + 36.86). `settle_to_ledger` makes the components add to what the journal credited. | #510 | 3 tests |
| D-CMP-5 | 2026-09-19 | A registration could be withdrawn, and the bill cancelled, under a live e-way bill. Both refused, naming it (NIC rule). | #511 | 2 tests |
| D-CMP-6 | 2026-09-19 | Re-registering a withdrawn invoice reissued the same IRN. Refused (NIC: a cancelled IRN is not reused); the withdrawn row stays as history. | #513 | backend + widget test |
| D-CMP-7 | 2026-09-19 | TCS on a back-dated receipt counted later receipts. The running total counts only receipts dated on or before it, within its financial year. | #514 | 1 test |
| D-CMP-8 | 2026-09-19 | Tax imports and the GST template committed row by row. All-or-nothing. | #516 | 4 tests |
| D-CMP-9 | 2026-09-19 | Tax deletes, restores, bulk actions and settings wrote no audit row, and a profile a rule uses could be deleted. All audited; that delete refused, naming the rules; reading settings writes nothing. | #519 | 5 tests |
| D-CMP-10 | 2026-09-19 | B2CL threshold, nil/exempt and DOCS were off. B2CL is Rs 1,00,000 from 2024-08-01 (Notification 12/2024-CT), 2,50,000 before; nil/exempt/non-GST go to Table 8 and 3B 3.1(c)/(e); DOCS shows cancelled. Regulatory reading by Claude -- confirm with a tax adviser. | #520 | 4 backend tests; widget test |
| D-CMP-11 | 2026-09-19 | A filed month changed when a bill in it was cancelled later. Decided by Claude: with no record of filing, the GSTR-1 due date (11th of the next month) stands in; a later cancellation stays in its month and is declared in the month it happened. | #522 | 2 tests |
| D-CMP-12 | 2026-09-19 | TCS was collected under s.206C(1H), omitted by the Finance Act 2025 from 2025-04-01. Receipts from that date are not applicable; earlier ones untouched. Regulatory reading by Claude -- confirm with a tax adviser. | #524 | 1 test |
| D-BUY-15 | 2026-09-19 | A purchase bill could lift its own quantity cap with `allow_over_invoice` (driven: 60 billed against a receipt of 6, approved 7,080.00). Decided by Claude, industry standard: no tolerance; the fields are gone. | #515 | revert fails |
| D-BUY-16 | 2026-09-19 | A goods receipt could lift the ordered-quantity cap (driven: 20 more received against an order already full). Fields gone; capped at ordered less received; received now counts COMPLETED and CLOSED receipts (closing one had un-received it). Firm-level tolerance is a backlog item. | #517 | 2 tests |
| D-BUY-17 | 2026-09-19 | A bill or return line was looked up by id alone (driven: a bill for 10 units never received, a return taking stock to -10, a return on a cancelled bill). A line must belong to the named, completed receipt of the same firm; a return on a bill needs it APPROVED or CLOSED; re-checked at approval and completion. | #521 | 2 tests |
| D-SELL-34 | 2026-09-19 | Revising a quotation dropped its freight. The editor loads the charge originally asked (charged plus waived), so free shipping is re-decided on save. | #523 | 2 widget tests |
| D-FIN-19 | 2026-09-19 | A purchase return raised from a receipt left a supplier credit nothing tracked (driven: vendor deleted with 236.00 of credit). The credit is derived from the return; `supplier_credit_applications` (migration 20260919_0141) records where it is set against a bill; listed and applied from Payments; counted by the delete guard. | #527 | 5 backend tests; 2 widget tests |
| D-CFG-1 | 2026-09-19 | Editing a conversion rule under a draft receipt changed its stock (driven: 10 PACK at factor 1 put 20 KG in). Decided by Claude, industry standard: a line keeps the factor it was written with. | #518 | `test_a_rule_edited_under_a_draft_receipt_leaves_its_stock_alone` |
| D-CFG-2 | 2026-09-19 | A typed number bypassed `manual_allowed` and a number typed ahead blocked the series. One `_issue_number` for all 13 creates: typed numbers only where the series allows them; issuing steps over taken numbers. | #525 | 2 tests; 19 fixture files marked `typed_document_numbers` |
| D-CFG-3 | 2026-09-19 | A point-value change re-priced points already earned (driven: 2600 ended at -100.00). IFRS 15: each batch keeps its rate; spending and take-backs value oldest first at their own rate. | #526 | 2 tests |
| D-CFG-4 | 2026-09-19 | Duplicate of D-BUY-15 and D-BUY-16 (the purchase over-receipt/over-invoice flags). | #515, #517 | see those rows |
| D-CFG-5 | 2026-09-19 | A field a category rule makes mandatory was accepted blank. One required set drives both checks. | #531 | 3 tests |
| D-CFG-6 | 2026-09-19 | The numbering series chosen was arbitrary. Active, then default, then oldest; an inactive series is refused; one default per type. | #528 | 2 tests |
| D-CFG-7 | 2026-09-19 | Toggling the yearly restart or retiring a series restarted at 000001 and then 409'd. A retired counter is revived; restart only for a genuinely new scope. | #529 | 2 tests |
| D-CFG-8 | 2026-09-19 | One type's series could issue another's numbers (and a pattern could spell `JV-`). A series' fixed head must not equal or prefix another type's. | #530 | 2 tests |
| D-CFG-9 | 2026-09-19 | Shared UOM rows were writable by any firm, and one firm could hang a level and barcode on another's product (driven in `firm_shared`). Decided by Claude, industry standard: shared UOM reference writes are platform-only (11 routes pinned); another firm's product answers 404. | #540 | 4 backend tests, pin test, widget test |
| D-CFG-10 | 2026-09-19 | Two firm-wide conversion rules could share a version. Partial unique index (migration 20260919_0143, renumbering live duplicates first) plus a service check. | #534 | 2 unit + 1 integration test |
| D-CFG-11 | 2026-09-19 | Whole-number units and rule rounding were not applied (driven: 1.5 PACK received; 0.33 vs 0.3333 KG). Refused by name on every document line and inbound movement; one rounding everywhere. `allow_fraction` deliberately not enforced (false on every product). | #537 | 4 tests |
| D-CFG-12 | 2026-09-19 | A place could be retired under customers, vendors and tax rows (driven: the customer's next save refused). Refused, naming what uses it. | #532 | 3 tests |
| D-CFG-13 | 2026-09-19 | Configuration changes the trail could not show (null firm, empty data, no rows). `record_change` records only what changed; sessions carry their firm so audit rows do too. Industry templates and profile UOM defaults: D-CFG-23. | #538 | 12 tests; integration audit tests |
| D-CFG-14 | 2026-09-19 | Sales-stage defaults were cleared when omitted and never validated. Left alone when omitted, validated, deletion of a default refused; the Stages dialog refuses to save after a failed read. | #536 | 4 backend + 2 widget tests |
| D-CFG-15 | 2026-09-19 | The Set up panel's MAIN was not the default warehouse, so bare bills were refused. Set up makes it the default, repairs stores with none, and readiness reports it. | #533 | 4 tests |
| D-CFG-16 | 2026-09-19 | With the note stage off, a draft bill adopted and cancelled a note a person raised. Notes the chain raises carry the raising bill (migration 20260919_0146); only that bill may ship or cancel them. | #539 | 2 tests |
| D-CFG-17 | 2026-09-19 | A sales manager could lift a credit block through the limit and grant points (owner's-decision row). Decided by Claude, industry standard (segregation of duties): a limit change needs `CUSTOMER_MANAGE_SETTINGS`, an adjustment `LOYALTY_MANAGE_SETTINGS`; the desktop shows the limit read-only. Accountants can no longer change a limit either. | #535 | 2 backend tests + widget test |
| TC-ROLE-009 | open decision | Deleting a role that people hold goes through with no warning, and they are left with an empty sidebar. | Recorded as the behaviour, not a defect; whether it should refuse or warn with a count is the owner's decision. |
