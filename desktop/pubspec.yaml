name: agency_desktop
description: Agency Platform reusable Flutter desktop foundation.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  csv: ^8.0.0
  desktop_drop: ^0.7.1
  excel: ^4.0.6
  file_selector: ^1.1.0
  flutter:
    sdk: flutter
  flutter_secure_storage: ^10.0.0
  # Opens the operating system's print dialog with a preview, and takes
  # pre-rendered bytes -- the backend already produces the PDF, so nothing
  # here lays a document out a second time.
  printing: ^5.13.0
  window_manager: ^0.5.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
