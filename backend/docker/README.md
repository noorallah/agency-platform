# Docker Assets

This directory is reserved for future deployment-specific Docker assets, such
as installer image variants and production orchestration overlays. The root
`Dockerfile` and `docker-compose.yml` are the standard local build entry points
for Phase 1.

For current backend module and migration status, see `..\README.md`.
